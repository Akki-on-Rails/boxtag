<%# <h1 style="font-weight:600;">QRCODE SCAN</h1> %>
<div class="box" style="margin:auto;margin-top:60px;">
  <video id="video" width="100%" height="100%" style="object-fit: contain;"></video>
</div>

<br>

<div style="text-align: center; width: 100%;">
  <a id="result" href="result" style="color:red;font-weight:600;text-decoration: underline red;text-align: center;width: 100%;"></a>
</div>

<%# <label><b>link:</b></label> %>




<%= javascript_pack_tag("basic-qr-code-reader") %>
<%= audio_tag "beep.mp3", id: "beep-audio"%>


<style>
.box {
  width:auto;
  height:auto;
  margin:0px;
  outline:0px solid;color:gray;
  outline-offset:28px;
  position:relative;
}
.box::before {
  content:"";
  position:absolute;
  top:0;
  bottom: 5px;
  left:0;
  width:0;
  background:#18c89b;
  box-shadow:0 0 70px 30px #18c89b;
  clip-path:inset(0);
  animation:
    x 0.7s ease-in-out infinite alternate,
    y 1.4s   ease-in-out infinite;
}

@keyframes x {
  to {
    transform:translateX(-100%);
    left:100%;
  }
}

@keyframes y {
   33% {
     clip-path:inset(0 0 0 -100px);
   }
   50% {
     clip-path:inset(0 0 0 0);
   }
   83%{
     clip-path:inset(0 -100px 0 0);
   }
}


</style>
